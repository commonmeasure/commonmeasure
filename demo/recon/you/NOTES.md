# You.com re-verification notes, 5 September 2026

One call with `YOU_API_KEY` (`X-API-Key`), `200`, 839 ms: `POST /v1/search`
with `count: 3` and `include_domains: ["www.w3.org"]`. Three results in
`results.web[]` with `snippets`, plus `metadata.search_uuid` and a
provider-side `latency`. No cost field: the charge is quoted from the
published per-call price.
